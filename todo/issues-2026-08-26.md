# 插件问题审查清单（2026-08-26）

> 仅列出，未修复。审查范围：`packages/` 下 4 个插件（pi-mcp-adapter、pi-subagents、pi-todo、pi-ask-user-question）。
> 方法：全量 typecheck + 单元测试（均通过：mcp-adapter 1330、ask-user-question 144、todo 187、subagents 22）+ 6 路深度代码审查。
> 严重度：中 = 建议尽快处理；低 = 常规处理；需确认 = 有证据但依赖运行时行为验证。

---

## 一、pi-mcp-adapter（数量 26，其中中 8 / 低 15 / 需确认 3）

### 认证 / 安全（A）

| # | 严重度 | 位置 | 问题 |
|---|--------|------|------|
| A1 | 中 | `mcp-auth-flow.ts:923-924` | `getValidToken` 在"已过期且无 refreshToken"时直接返回已过期 token（注释称"assume valid"），与 `oauth-handler.ts:20-22`（过期返回 undefined）行为矛盾。下游持续收到 401 且部分调用方不会重新认证，静默失败。应返回 null（仅"无过期信息"时才返回）。 |
| A2 | 中·需确认 | `mcp-auth.ts:354-390` | Linux keyring 恢复路径每次以 `keyctl session - node helper` 启动子进程（新建临时 session keyring），写入后进程退出即被内核回收，凭据可能不持久——表现为重登"成功"但下次仍显示未认证。取决于 `@napi-rs/keyring` 实际使用的 keyring 类型。 |
| A3 | 低 | `mcp-callback-server.ts:437-443` | `waitForCallback` 的 5 分钟超时 timer 未 `unref()`，回调未发生时拖住事件循环最长 5 分钟，与 server 已 unref 的设计矛盾。 |
| A4 | 低 | `mcp-callback-server.ts:208-209` | `new URL(req.url, "http://"+req.headers.host)` 无异常保护，畸形 Host（如 `localhost:99999`）触发同步异常 → uncaughtException 崩溃（本地 DoS，loopback 可达）。 |
| A5 | 低 | `mcp-auth-flow.ts:805` | 完整 authorizationUrl 打印到终端（含 state、code_challenge、用户自定义 `authorizationParams`，SDK 注释明示 "may contain sensitive params"）。 |
| A6 | 低 | `mcp-auth-flow.ts:564-600,658-674` | 手动 `auth-complete` 粘贴裸 code 时完全跳过 state 校验（仅 params 分支校验 expectedState），与要求完整回调 URL 的自动路径不一致，state 防 CSRF 在手动流中缺失。 |
| A7 | 低·需确认 | `mcp-auth.ts:158-162` + `mcp-auth-flow.ts:863-925` | `authEntryCache` 跨进程陈旧：刷新无单飞去重，多进程同时刷新同一 server 时旧 refresh token 被判 `invalid_grant`，表现为间歇性"未认证"。 |
| A8 | 低 | `mcp-oauth-provider.ts:296-307` | config 预注册 `clientId` 会用 stub 整条替换之前动态注册的 clientInfo，动态下发的 clientSecret/redirectUris 被覆盖丢失，之后刷新持续 `invalid_client`。 |
| A9 | 低 | `mcp-auth.ts:733-735,794-800` | 明文 `tokens.json` 在仅做状态读取（`inspectAuthForUrl` 恒定 `migrateLegacy:false`）时不会被迁移/删除，明文 token 长期驻留磁盘。 |
| A10 | 低 | `mcp-callback-server.ts:387-390` | strictPort 重绑时 `await previousServer.close(cb)` 无超时，keep-alive 残留连接可阻塞入口最多 5s，期间新端口返回 404。 |

### 服务器生命周期 / 代理（B）

| # | 严重度 | 位置 | 问题 |
|---|--------|------|------|
| B1 | 中 | `npx-resolver.ts:233-247` | Windows 上找不到 `.cmd`/`.ps1` shim（只找无扩展名 binLink），非 JS 原生二进制直接 spawn 包内原始文件，启动失败（跨平台残缺）；需确认 Windows 是否在支持矩阵内。 |
| B2 | 中 | `lifecycle.ts:109-117` + `server-manager.ts:228` + `init.ts:132` | 健康检查被"永不完成 connect"的服务器永久卡死：`requestTimeoutMs` 未配置时 connect 无请求超时，`activeHealthCheck` 永不清空 → 之后**所有**健康检查 tick 全部跳过，`ensureConverged`/消息投递也挂死。建议给健康检查的 connect 注入兜底超时。 |
| B3 | 中 | `server-manager.ts:299-304` + `683-697` + `mcp-probe.ts:169-180` | connect 失败（含 abort/close 导致的失败）都会走 `enrichHttpConnectionError` 做完整 HTTP 探测（独立 5s 超时 × 最多 3 请求），关闭/重载时每个失败连接最多拖慢 ~15s。abort 后应先短路。 |
| B4 | 中·需确认 | `init.ts:141-145` | sampling 闭包在 connect 后仍直读 `ctx.model`/`ctx.signal`（未快照），会话销毁后 getter 抛 stale-ctx 错误，可能污染一轮对话（取决于 host 是否总是先同步 stop owner）。 |
| B5 | 低-中 | `session-recovery.ts:47-63,118-156` | 重试依赖"404/未初始化必须在处理请求前返回"的规范保证，非规范服务器（或 400+`-32000` 匹配过宽）会 double-execute 工具副作用（代码注释已声明为已知权衡）。 |
| B6 | 低-中 | `npx-resolver.ts:169-185,211-231` | 非精确版本（`^1.2.3`/tag）缓存按 mtime 取"最新"目录，多版本共存时可能解析错版本，24h TTL 内不自我纠正。 |
| B7 | 低 | `session-recovery.ts:50-63` + `proxy-modes.ts:1285-1290` | 连接运行中崩溃（ConnectionClosed）不触发任何自动重试；stdio 服务器无周期性活性检查，体验依赖手动重试。 |
| B8 | 低 | `metadata-cache.ts:76-99` + `init.ts:160-176` | 缓存顶层校验过浅：`prompts`/`tools` 字段被手工改成非数组时，`reconstructPromptMetadata` 处 `.filter` TypeError → 整个初始化崩溃。 |
| B9 | 低 | `metadata-cache.ts:100-132` | `saveMetadataCache` 整文件读-改-写（tmp+rename），多进程并发更新不同条目时后写者覆盖丢条目。 |
| B10 | 低 | `proxy-modes.ts:1170-1180` + `init.ts:252-270` + `server-manager.ts:238-260` | connect 被 `close()`/`reconnect()` 的 attemptController 主动 abort 时，`isAbortError`（按 error.name）仍判为失败 → `recordFailure` → 60s 失败回退，合法关闭后连接被拒绝重连。 |
| B11 | 低 | `server-manager.ts:388-393` + `lifecycle.ts:266-268` | keep-alive 的 tools/list 刷新硬上限 5s：健康但列表慢（>5s）的服务器每次刷新超时 → 指数退避到 5min，期间元数据变更永远看不到。 |
| B12 | 低 | `server-manager.ts:392` + `mcp-probe.ts:89` | 每次健康检查新建 `AbortSignal.timeout()` 定时器，提前完成后不显式清理（Node 部分版本不 unref），长时间运行持续产生定时器。 |
| B13 | 低 | `server-manager.ts:1113-1120` | 单个服务器 `close()` 不清理其注册的 `uiStreamListeners`（仅在 closeAll 清空），同 token 被复用时会向错误目标（旧 serverName 闭包）发通知。 |
| B14 | 低 | `npx-resolver.ts:257-276` | `forceNpxCache` 超时/中止只 kill npm 父进程，npm 再派生的孙进程可能成孤儿（窗口极小，影响有限）。 |
| B15 | 低 | `server-manager.ts:707-733` | `connectClientWithAbort`：abort 恰落在 connect resolve 与移除 listener 之间时，`closeTransport()` 的 promise 无人 await，close 抛错会成为 unhandled rejection（纳秒级窗口）。 |

### UI / 工具注册 / 命令（C）

| # | 严重度 | 位置 | 问题 |
|---|--------|------|------|
| C1 | 中 | `ui-session.ts:273-284,460,397-421,482-491` | 并发 UI 工具调用互相替换 `state.uiServer`：2 秒窗口内连续调用两个不同 UI 工具，前一会话的 `isActive` 变 false → 结果静默丢弃、onComplete 不清理 → stream listener 永久泄漏、SSE 服务器只能靠 60s 看门狗兜底关闭。 |
| C2 | 中 | `ui-server.ts:571-583` + `host-html-template.ts:275` | `/proxy/ui/open-link` 仅 `new URL()` 语法校验，`file:`/`smb:`/`javascript:` 等任意协议放行；UI 资源是受沙箱但**允许脚本**的第三方内容，可诱导宿主页打开本地文件/外部资源。建议只放行 http/https。 |
| C3 | 中 | `elicitation-handler.ts:49-50,321-325` + `sampling-handler.ts:65,102-108` + `commands.ts:118-119,146` | 确认对话框与 `/mcp prompts`、`/mcp tools` 输出**未做终端转义**（面板路径有 `sanitizeRowContent`，命令路径没有）：不可信的 MCP 服务器可注入 ANSI/OSC 序列（超链接、改色、占屏）伪装系统输出。 |
| C4 | 中 | `mcp-output-guard.ts:116-135` + `tool-registrar.ts:192-197` | `image` 内容块的 base64 data 完全不参与 50KiB/2000 行预算，原样透传进模型上文——恶意/出错服务器可用单个 image 块注入任意大小数据，绕过输出防护（有 `imageBlocksPassedThrough` 标记，属有意折衷但无大小上限）。 |
| C5 | 低-中·需确认 | `ui-server.ts:379-396,98-113` | SSE 客户端断连时 流式 `res` 无 `error` 监听，`res.write` 的异步 'error' 事件可能成为 uncaughtException（Node 版本相关；加监听是无害防御）。 |
| C6 | 低-中 | `ui-server.ts:147,161` | `sessionMessages.prompts`/`notifications` 无上限累积（contexts 有 20 条上限），被 UI 全文回传给模型：内存增长 + 上下文被第三方 UI 放大（eventLog 有 128 上限，消息没有）。 |
| C7 | 低 | `elicitation-handler.ts:105-121,218` | `type:"string"` 但 `enum:[1,2]`（或 oneOf const 非字符串）时校验永远失败 → notifify → 无限重新询问，用户只能取消整单（死循环）。 |
| C8 | 低 | `mcp-trace.ts:206-211` + `server-manager.ts:493` | trace 文件每次实例/重启新建，从不清理，开启后磁盘持续增长。 |
| C9 | 低 | `index.ts:749-756` | `/mcp reconnect` 用 `parts[1]` 单 token 解析服务器名（其余子命令均用 `rest.join(" ")`），含空格服务器名无法重连。 |
| C10 | 需确认 | `namespace-tools.ts:195-204` | 每次元数据更新/重连 `syncToolSurface` 无条件重新 `registerTool`（无 fingerprint 比对），与 direct-tools 的冻结语义不一致；若宿主按注册事件重建 system prompt 会产生提示词搅动。 |
| C11 | 需确认 | `ui-session.ts:164-165` + `index.ts` session_shutdown | Glimpse 窗口（模块级单例）不在 `session_shutdown` 回收，会话结束后指向已关闭 UI 服务器。 |
| C12 | 需确认 | `sampling-handler.ts:189-192` + `elicitation-handler.ts:49-89` | 确认对话框未绑定 abort signal，会话中止后仍悬挂至用户作答（残留 UI，无状态损坏）。 |

### 已排除的疑点（核实后无问题）
- host-html-template 转义正确（safeInlineJSON/escapeHtml 完备，无 XSS）；iframe 沙箱无 allow-same-origin。
- 事件循环/循环依赖无问题；SDK StdioClientTransport close 的 SIGTERM→SIGKILL 时序完备。
- callback server 的 stop/restart generation 检查安全；PKCE/state/iss 校验正确。

---

## 二、pi-subagents（数量 13，其中中 4 / 低 7 / 需确认 2）

| # | 严重度 | 位置 | 问题 |
|---|--------|------|------|
| S1 | 中 | `src/runs/execution.ts:246-258` | abort 路径只发一次 SIGTERM，无 SIGKILL 兜底升级（timeout 路径有 3s 升级）；子进程忽略/阻塞 SIGTERM 时工具调用永不返回，前台会话卡死。注意：`proc.kill("SIGTERM")` 在 Windows 上等同于强制杀，此问题仅影响 POSIX。 |
| S2 | 中 | `src/runs/async.ts:300-305,455-470` | 父进程崩溃后孤儿后台子进程：`started.json` 不保存 pid → 新父进程无法终止它；run 结果无人 replay、无 result.json，与 README "survive parent reloads…result will be delivered separately" 承诺不符（运行中的任务结果不会交付）。 |
| S3 | 中·需确认 | `src/agents/agents.ts:66-67` + `src/runs/pi-args.ts:113-125` | frontmatter `thinking` 自由字符串无校验（工具参数有 TypeBox enum），`thinking: High` 等非法值直通子进程 CLI（`--model x:High`/`--thinking <值>`），子进程启动报错晦涩。（已确认：`pi-args.ts` 侧无校验逻辑） |
| S4 | 中 | `src/runs/async.ts:199-207` + `src/extension/index.ts:66-80` | `persistAndEmit` 直接调用 `onComplete`/`events.emit` 无 try/catch；`onComplete` 最终执行 `pi.sendMessage(triggerTurn)`，抛错时 interval 轮询路径可崩父进程、launch 路径可使工具调用失败。 |
| S5 | 低-中 | `src/agents/agents.ts:39-41` | `truthy()` 只排除 `false/0/no`，YAML 1.1 合法假值 `off` 被解析为 true → `inheritSkills: off` 变成"开启"（已实测确认）。 |
| S6 | 低 | `src/slash/commands.ts:110` + `src/runs/executor.ts:109` | `/run` 用 `Date.now()` 作 runId，同毫秒两次调用撞相同 runId → fork 会话文件互相覆盖、两个子进程写同一 session.jsonl。 |
| S7 | 低 | `src/runs/execution.ts:272-277` | `progress.recentTools` 无界增长（recentOutput 有 splice 上限，recentTools 从不裁剪），长任务时内存常驻增长。 |
| S8 | 低 | `src/runs/async.ts:303,461` | `status.json` 只写 `running`/`interrupted`，失败/完成的 run 永不更新，且全包无代码读取——磁盘状态文件始终失真（死数据）。 |
| S9 | 低 | `src/runs/pi-args.ts:84-93` | `mkdtempSync` 之后任一步骤失败（写 task.md/磁盘满/权限）时临时目录泄漏（cleanupTempDir 只覆盖成功路径）。 |
| S10 | 低 | `src/runs/async.ts:394-400` vs `:372-374` | 2s 轮询 finalize 与 `exit` 事件回调竞态：poller 先触发时 `exitCodes.get` 为 undefined → `exitCode ?? (settled ? 0 : 1)` 把实际非 0 退出记成 0（影响限于 stat 字段）。 |
| S11 | 低 | `src/shared/types.ts:97-108`、`src/shared/utils.ts:24`、`src/agents/agents.ts:24,236`、`builtin-names.ts:13`、`pi-args.ts:36,185`、`async.ts:31,385`、`pi-args.ts:161-166`、`agents/scout.md:9`、`researcher.md:9` | 死代码/未消费字段：`SubagentState`/`createInitialState`、`findLatestSessionFile`、`BUILTIN_AGENT_NAMES`（仅 re-export）、`SUBAGENT_PARENT_CWD_ENV`/`SUBAGENT_ASYNC_STARTED_EVENT`（包内无消费者）、`--session-dir` 分支不可达、scout/researcher 的 `output:` frontmatter 无消费且"write output to the provided path"无代码提供路径。 |
| S12 | 需确认 | `src/runs/execution.ts:312-330` | 若同一终态消息出现两个 `message_end`（分片重发），usage 翻倍、messages 重复；需对照 `pi --mode json` 实际事件协议验证。 |
| S13 | 需确认 | `src/runs/fork-context.ts:48-56` | 父会话正在追加写入时 readFileSync 可能读到半行（JSON.parse 被 catch 丢弃）→ fork 少最后一两轮上下文（代码已容忍，仅提示截断窗口）。 |

### 已排除
- Shell/命令注入：全部 spawn 参数数组，无 `shell:true`；Windows 路径与 `pi.exe` 匹配、`windowsHide` 均已处理。

---

## 三、pi-todo（数量 7，其中中 1-2 / 低 5）

| # | 严重度 | 位置 | 问题 |
|---|--------|------|------|
| T1 | 中 | `config.ts:237`（调用点 `index.ts:137`、`todo-overlay.ts:177`） | `config.collapseKey?.trim()` 只保护 null/undefined：数字/布尔/数组等非字符串值直接抛 `TypeError: trim is not a function`——与函数自身注释和 README "silently dropped back to its default. Never an error." 矛盾。扩展工厂调用点抛错会终止整个扩展注册；overlay 渲染路径调用点可直接崩溃 widget。（已实测确认：`{"collapseKey": 123}` 抛 TypeError） |
| T2 | 中低 | `index.ts:193,164`（异常源头 `index.ts:119,updateTodoOverlay`） | `session_start`/`session_compact`/`session_tree` 路径对 `updateTodoOverlay` 无 try/catch（tool_execution_end 有完整兜底策略）：瞬时模块加载失败产生未处理 rejection，且该次 overlay 刷新（含 resetCompletedDisplayState）静默丢失；latched stale-namespace 错误在这两条路径以未捕获形式上抛，用户看不到"restart Pi"指引。 |
| T3 | 低 | `todo-overlay.ts:205` + `state/selectors.ts:88-105` | 溢出摘要把截断尾部一律报成 "N pending"，实际可能有 in_progress 任务——overlay 对隐藏内容的唯一说明报错进行中状态。 |
| T4 | 低 | `state/task-graph.ts:18` + `state/state-reducer.ts:178-188` | 环检测包含已删除（tombstone）任务的残留 blockedBy 边：A←C←B(已删←A) 场景下对活任务添加合法依赖被误判为环拒绝（删除中间任务后悬空依赖的常见路径）。 |
| T5 | 低 | `state/state-reducer.ts:220` | 自动降级（demote 到 pending）保留旧 `activeForm`，之后未传新 activeForm 置回 in_progress 时过期标签复活显示。 |
| T6 | 低 | `tool/sanitize.ts:14-31` | 7-bit 形式的 DCS/PM/APC/SOS（`ESC P/^/_/X`）只剥掉 `ESC+字母`，引入符与 ST 之间的整段载荷文本残留（8-bit 形式会被清掉）。与注释宣称的 "Complete escape sequences are dropped whole" 不完全一致；残余为纯文本，不构成终端控制注入，攻击面很小。 |
| T7 | 低·死代码 | `state/selectors.ts:108` | `ACTIVE_STATUSES` 全包无任何引用（write-only 导出）。 |

### 已排除
- config.test.ts 第 38 行 `"ctrl+o+?"` 断言：经实测 `isValidCollapseKeySpec` 返回 false（`o` 非合法修饰键），测试与实际行为一致，无矛盾。

---

## 四、pi-ask-user-question（数量 10，其中中 2 / 低 6 / 需确认 2）

| # | 严重度 | 位置 | 问题 |
|---|--------|------|------|
| Q1 | 中 | `config.ts:160-173` + `key-router.ts:310-314` + `ask-user-question.ts:195-207` | `collapseKey` 配置无语义校验：`"n"`（吞笔记键）、`"enter"`（吞确认）、`"esc"`（吞取消）等均可合法通过且被顶层拦截优先消费——用户配错后问卷"按键失灵/行为错乱"且无任何警告。建议拒绝功能保留键或 console.warn。 |
| Q2 | 中 | `rpc-fallback.ts:77` vs `:124` | RPC 路径 preview 在对话框标题截断 600 字符，但答案 `preview` 回传全文——模型拿到用户从未见过的内容，可能引用不可见信息作答（TUI 路径同样存在 contentBudget 截断 vs 回传全文的形态，需确认是否为有意）。 |
| Q3 | 低 | `tool/validate-questionnaire.ts:36-54` + `tool/types.ts:35-110` | 运行时复查不完整：`> MAX_OPTIONS`、`MAX_HEADER/LABEL_LENGTH`、`question`/`description` 长度均无复查（question/description 在 schema 也无 maxLength），只依赖宿主 schema 校验兜底；超限输入直接进渲染，长 question 可破坏跨 tab 高度对齐。 |
| Q4 | 低 | `view/tab-content-strategy.ts:104-117` + `view/dialog-builder.ts:151-157` | 残差 spacer 只补偿 body 差不补偿 heading 差，多行 question 标题时问题 tab 与 Submit tab 高度突变，往返切换跳动（需要与 Q3 的超长输入叠加才触发）。 |
| Q5 | 低 | `view/components/wrapping-select.ts:60-66` vs `231-241` | 注释声称 "active rendering wins (no double-mark)"，实现却无条件加 `✔`：回到已答 tab 时焦点行同时显示 `❯` 指针和 `✔`（注释过期或产品意图需二选一）。 |
| Q6 | 低 | `tool/response-envelope.ts:36-37` | 多问题模式下故意提交**空答案**被表达为 "User declined to answer" + `cancelled:true`，与用户按 Esc 取消在模型视角完全不可区分（真实事件是"提交空答卷"）。 |
| Q7 | 低·死代码 | `state/key-router.ts:58`（`allAnswered`，且与 `projections.ts:98` 重复实现）、`tool/types.ts:146`（`isQuestionnaireResult`）、`view/components/wrapping-select.ts:91`（`setNumbering`） | 仅测试消费或无任何调用。 |
| Q8 | 低 | `tool/validate-questionnaire.ts:41` | 保留标签（`other`/`type something.`）校验大小写敏感：`"Other"`/`"TYPE SOMETHING."` 可绕过，与自动追加 sentinel 行形成"双入口"，README 声称的 "rejected at runtime" 只对精确匹配成立。 |
| Q9 | 低·需确认 | `ask-user-question.ts:243-245` + `262-263` | 无 dialog 原语的老 RPC 宿主的 `ui.custom` 立即返回 undefined：`blocked{true}` 与 `blocked{false}` 几乎同时发出，监听插件收到零时长"阻塞"闪烁。 |
| Q10 | 需确认 | `ask-user-question.ts:322` | `execute` 完全忽略 `_signal`：run 中断/切会话时 `ctx.ui.custom` 悬置行为未定义，可能残留 overlay（取决于宿主是否自动关闭 custom overlay）。 |

### 已排除
- 键盘路由全部分支、raw 输入 double-toggle、onHandle→factory 顺序、事件监听清理（finally 移除/unref）、渲染死循环/宽度计算防护——均已核实无问题。

---

## 五、项目层面（本次审查发现）

| # | 严重度 | 位置 | 问题 |
|---|--------|------|------|
| P1 | 中 | `git status` / `packages/pi-mcp-adapter/` | **整个 pi-mcp-adapter 包未加入 git 版本控制**（untracked，含 60+ 源文件、dist、示例；仅其内部 .gitignore 生效），而根 README 已引用它——存在整包丢失风险，应立即 `git add`。 |
| P2 | 低 | 根目录 `package-lock.json` | 空的 lockfile（`packages: {}`），无配套根 package.json/workspaces，属 npm 遗留杂物，建议删除或配 workspaces。 |

---

## 统计

| 包 | 中 | 中·需确认 | 低 | 低-中 | 需确认 | 合计 |
|----|----|-----------|-----|-------|--------|------|
| pi-mcp-adapter | 7 | 1 | 14 | 2 | 2 | 26 |
| pi-subagents | 3 | 0 | 7 | 1 | 2 | 13 |
| pi-todo | 1 | 0 | 6 | 0 | 0 | 7 |
| pi-ask-user-question | 2 | 0 | 6 | 0 | 2 | 10 |
| 项目层面 | 1 | 0 | 1 | 0 | 0 | 2 |
| **合计** | **14** | **1** | **34** | **3** | **6** | **58** |

## 建议优先处理（影响面排序）

1. **B2** 健康检查永久卡死（瘫痪 keep-alive 与消息投递）+ **C1** UI 会话并发替换（结果丢失+泄漏）
2. **A1** 过期 token 静默失败、**C2** open-link 任意协议、**C3** 终端注入、**C4** image 绕过输出防护
3. **S1** abort 无 SIGKILL 兜底、**S2** 孤儿后台进程不可终止
4. **T1** collapseKey 非字符串崩溃 + **Q1** collapseKey 语义冲突（同族配置健壮性）
5. **P1** mcp-adapter 未入库（仓库层面，最先处理）